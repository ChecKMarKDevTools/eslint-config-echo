# Examples

This folder is intentionally minimal to avoid duplicating the primary docs.

For installation, usage details, and badges, see the repo root README: `../README.md`.

## Files

- `eslint.config.mjs` — ESLint v9 flat config (recommended)
- `eslint.config.cjs` — ESLint v9 flat config (CommonJS projects)
- `.eslintrc.cjs` — ESLint v8 legacy config (extends this package)
